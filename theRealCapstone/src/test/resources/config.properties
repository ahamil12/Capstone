# if having trouble connecting and you're using mac/linux, try this connection string instead:
# url=jdbc:mysql://localhost:3306/chef_db?serverTimezone=EST5EDT

url=jdbc:mysql://localhost:3306:/chef_db

username=root

# change password if not correctly written here
# password=root
password=root